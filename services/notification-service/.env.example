NODE_ENV=development
PORT=4004
DB_HOST=postgres
DB_PORT=5432
DB_NAME=cst_system
DB_USER=postgres
DB_PASSWORD=change_me_in_secrets_manager
JWT_ACCESS_SECRET=replace_with_openssl_rand_base64_48
RABBITMQ_URL=amqp://rabbitmq:5672

# SMTP — if left unset, emails are logged instead of sent (safe dev default)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
SMTP_FROM="Support Desk <no-reply@example.com>"
FRONTEND_URL=http://localhost:3000

CORS_ORIGIN=http://localhost:3000
LOG_LEVEL=info
